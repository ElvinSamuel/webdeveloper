// Null vs Undefined

// Variables that are DECLARED but not initialized are undefined
var name;
var age;

// Null is "explicitly nothing"
console.log(someNonVariable);
// --> null (not declared, and by association not defined)
var currentPlayer = "charlie";
var currentPlayer = null; // game over